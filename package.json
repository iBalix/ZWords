{
  "name": "zwords",
  "version": "1.0.0",
  "description": "ZWords - Jeu de mots croises multijoueur temps reel pour ZLAN 2026",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build:api": "cd apps/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "build": "npm run build:api && npm run build:web",
    "start:api": "cd apps/api && npm start",
    "start:web": "cd apps/web && npm run preview",
    "install:all": "npm install && cd apps/api && npm install && cd ../web && npm install"
  },
  "keywords": [
    "crossword",
    "multiplayer",
    "realtime",
    "socket.io",
    "zlan"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
